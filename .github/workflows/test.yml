name: Test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  audit-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run isolated safety tests
        run: bash tests/run.sh
